#include <iostream>
#include <vector>
#include <algorithm>
#include <cstring>
#include <queue>
using namespace std;

int main() {
    int N, M, P, Q; // N planets, each w/ M cities, P flights connecting a and b city on each planet, and Q portals connecting x and y city on different planets
    cin >> N >> M >> P >> Q;
    
}
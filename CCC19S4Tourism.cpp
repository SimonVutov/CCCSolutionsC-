#include <vector>
#include <iostream>
#include <algorithm>
#include <cmath>
using namespace std;

int main() {
    int N, M; // len of nums, max nums per slice
    cin >> N >> M;
    vector<int> A (N);
    for (int i = 0; i < N; i++) cin >> A[i];


    int Splits = ceil(N / M);
}

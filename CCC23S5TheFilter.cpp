#include <iostream>
#include <unordered_set>
#include <string>
#include <functional>

using namespace std;

bool go(int x, int n) {
    if ((double) x / (double) n < )
}

int main() {
    int n;
    cin >> n;

    for (int i = 0; i <= n; i++) {
        if (go(i, n)) cout << i << endl;
    }
}
